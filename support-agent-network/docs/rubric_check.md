# Rubric Check — OrbitDesk Support Agent Network

> **Status:** Draft — updated after each phase completes.
> Maps each assignment rubric line item to the file/line/test that satisfies it.

---

## Triage

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| Query classification into 4 required categories | `src/agent/nodes/triage.py` — 4-step hybrid (deterministic → embedding similarity) | ✅ |
| Hybrid deterministic + model classification | `triage.py::_check_out_of_scope`, `_check_clarification_needed`, `_check_escalation_needed` (deterministic); `_classify_by_embedding` (model) | ✅ |
| Out-of-scope: no generation call | `graph.py::route_after_triage` returns `"finalise"` for `out_of_scope`; `test_routing.py::test_out_of_scope_does_not_route_to_generation` | ✅ |

## Retrieval

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| Local embedding index (real HF model, not keyword stub) | `src/agent/nodes/retrieval.py::_VectorIndex`; model `sentence-transformers/all-MiniLM-L6-v2` loaded via `sentence_transformers` | ✅ |
| Cosine similarity retrieval | `retrieval.py::_VectorIndex.query` — pre-normalised dot product | ✅ |
| Multi-document retrieval visible in sources | `test_end_to_end_cases.py::TestCase1Answerable::test_multi_doc_sources` asserts KB-003 or KB-004 in sources | ✅ |
| Relevance threshold / insufficient-evidence signal | `retrieval.py` RETRIEVAL_THRESHOLD=0.35; `retrieval_sufficient=False` when below | ✅ |
| No managed vector DB | Linear numpy scan only; no external service | ✅ |

## Generation

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| Real local HF causal LM (not stub) | `models.py::load_generator` loads `Qwen/Qwen2.5-0.5B-Instruct` via `transformers.AutoModelForCausalLM` | ✅ |
| Constrained prompt (answer only from evidence) | `generation.py::_build_prompt` — explicit instruction + sentinel | ✅ |
| Load time + per-call latency recorded | `models.py::ModelTiming`; surfaced in CLI `--timing` flag and `get_timing_report()` | ✅ |
| Generation skipped for out_of_scope / requires_clarification | Graph edge `route_after_triage` returns `"finalise"` — generation node never reached | ✅ |
| Deterministic/model separation | `generation.py::_build_prompt` (deterministic, no model calls) vs `_call_model` (model only) | ✅ |

## Verification

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| Every claim traceable to evidence | `verification.py::_compute_overlap_score` — trigram overlap ≥ 20% | ✅ |
| At least one source for answerable | `verification.py::_has_evidence_for_answerable` | ✅ |
| Schema validation | `verification.py::_schema_prevalidation_check`; full validation in `finalise.py` | ✅ |
| Superseded-guidance check | `verification.py::_check_superseded_content` — CASE-0914 regex patterns | ✅ |
| Retry with informed hint | `graph.py::route_after_verification` → back to `generation` with `regeneration_hint` | ✅ |
| Safe-failure (not exception) when max retries exhausted | `finalise.py` — `classification="safe_failure"`, `SAFE_FAILURE_ANSWER` text; `try/except ValidationError` | ✅ |

## State and Schema

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| Shared typed state | `src/agent/state.py::AgentState` — TypedDict with 19 fields, all documented | ✅ |
| `attempt_count` field | `state.py` field with docstring; incremented in `generation_node` | ✅ |
| `node_trace` field (execution log) | `state.py`; every node appends its name first; written to `logs/run_*.jsonl` | ✅ |
| Pydantic output schema | `src/agent/schema.py::SupportResponse` with cross-field validators | ✅ |
| Schema extensions documented | `design.md §9` and `schema.py` docstring | ✅ |

## Graph / Orchestration

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| LangGraph StateGraph with real conditional routing | `src/agent/graph.py` — `StateGraph(AgentState)` with `add_conditional_edges` | ✅ |
| Not a linear chain | 3 conditional edge functions; back-edge from verification to generation | ✅ |
| Loop guard (attempt_count ceiling) | `graph.py::route_after_verification` and `route_after_retrieval` check `attempt_count >= MAX_ATTEMPTS` FIRST | ✅ |
| Loop guard in edge, not node | Design decision documented in `design.md §6`; loop-guard unit test in `test_routing.py` | ✅ |
| Graph diagram | `docs/graph_diagram.png` — generated by `scripts/generate_graph_diagram.py` | ✅ |

## Testing

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| At least one wording-independent routing test | `test_routing.py::TestRouteAfterTriage::test_out_of_scope_does_not_route_to_generation` (and 18 others) | ✅ |
| Routing tests independent of model output | All `test_routing.py` tests operate on edge functions with synthetic state — no models loaded | ✅ |
| Verification unit tests | `tests/test_verification.py` — 24 tests, all deterministic | ✅ |
| 5 required end-to-end cases | `tests/test_end_to_end_cases.py` — all 5 sample questions | ✅ |
| Retry path exercised in test | `TestCase5OutOfScope::test_node_trace_skips_generation`; `TestCase1Answerable::test_node_trace_includes_generation` | ✅ |

## Models and Hardware

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| Exact model names and revisions | `models.py` constants `EMBEDDING_MODEL_ID`, `GENERATOR_MODEL_ID`, both with `revision="main"` | ✅ |
| Model load time recorded | `ModelTiming.embedding_load_s`, `generator_load_s` | ✅ |
| Per-query latency recorded | `ModelTiming.generation_calls` list | ✅ |
| Hardware documented in README | README §Hardware — actual machine specs + measured timings | ⏳ (Phase 9) |
| CPU-safe (no unconditional GPU assumption) | `models.py::load_generator` — `torch.cuda.is_available()` check; float32 CPU fallback | ✅ |

## Offline Operation

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| 100% local inference after download | `models.py` — HF cache only; `HF_HUB_OFFLINE=1` enforcement | ✅ |
| Demonstrable offline operation | `scripts/offline_check.py` — socket monkeypatch + HF offline flags | ✅ |
| No hosted LLM API calls | No openai/anthropic/cohere/gemini in requirements.txt or codebase | ✅ |

## Documentation

| Requirement | Satisfied by | Status |
|-------------|-------------|--------|
| Architecture summary | README §Architecture | ⏳ (Phase 9) |
| AI assistance disclosure | README §AI Assistance Disclosure | ⏳ (Phase 9) |
| Known limitations | README §Known Limitations; `docs/tradeoffs.md` | ⏳ (Phase 9) |
| No secrets in repo | `.gitignore` excludes `.env`; `.env.example` has no values | ✅ |
| Structured logs per node | `logging_config.py::log_node_entry`; `logs/run_<timestamp>.jsonl` | ✅ |

## Open Items

- [ ] **README** full content (Phase 9)
- [ ] **docs/graph_diagram.png** generated (Phase 5 — run `scripts/generate_graph_diagram.py`)
- [ ] **outputs/** sample JSON files (Phase 8 — run `scripts/run_demo.py`)
- [ ] **Hardware timing numbers** filled into README from actual measured run
- [ ] **End-to-end test run** with models (Phase 7 final verification)
