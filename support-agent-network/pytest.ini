[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Default timeout for slow model tests
timeout = 600
# Markers
markers =
    slow: marks tests as slow (requires model loading, deselect with -m "not slow")
addopts = -v --tb=short
